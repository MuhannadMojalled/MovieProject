this project was developed using python version "3.10.5", it might not work on other vesions without some modification.
all required libraries are specified in requiremnets.txt,
to install all requirements follow the following steps: cd to the project's directory, then run "pip install requirements.txt". 
all assets are loacted inside the assets folder. 